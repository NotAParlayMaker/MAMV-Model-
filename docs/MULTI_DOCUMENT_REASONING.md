# Multi-document reasoning
`answer_files` ingests an ordered immutable collection, suppresses duplicate content hashes, and retains document and chunk IDs. Retrieval can cap chunks per document and records choices in the frame. Retrieval scores are ranking signals only, never evidence support.
