# Contradiction candidates
Candidates are bounded heuristic/model proposals (`potentially_conflicting`, `temporally_distinct`, and related relations), not verification results. Numeric and polarity differences are surfaced; date differences are kept temporally distinct rather than automatically called contradictions.
