# Frame compatibility

Use `compare_inference_frames(a, b)`. Artifact and effective-context equality are required for direct comparison. Model changes require re-evaluation and source-context changes require re-grounding. A strategy change is disclosed but does not alone make answers incomparable.
